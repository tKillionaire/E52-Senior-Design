"""
make it so it asks to upload a file or start data collection with sensors and CNC
start with button push
"""


def function getData():
    #run microcontroller, and collect data into matrix [x,y,z, B-field]
    #return matrix
    return